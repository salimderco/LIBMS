kind = "web"
previewPath = "/"
title = "Faculty Library Management System"
version = "1.0.0"
id = "artifacts/flms"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20670

[services.development]
run = "pnpm --filter @workspace/flms run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/flms", "run", "build" ]
publicDir = "artifacts/flms/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20670"
BASE_PATH = "/"
